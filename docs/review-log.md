# Review log

| Date | Branch | Class | Reviewer | Verdict | Catches | Escapes | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-13 | `codex/reconcile-resolved-verdict` | User-visible verdict refresh lifecycle | Codex, strongest adversarial review | Merge ready | 2 | 0 | Reproduced queued refresh overlap, queue rejection redelivery, and an older retry behind a newer success. The final hostile sequence kept every active refresh visible and suppressed completed redeliveries. |
| 2026-09-10 | `rules/review-quality` | Model review policy | Codex, high | Merge ready | 0 | 0 | The policy test failed without the new instructions and passed after restoration. Full tests, checks, fresh signed build, signature verification, and the live merge tree passed. |
| 2026-09-10 | detached `6b2ef89` | User-visible review lifecycle | Codex, high | Merge ready | 0 | 0 | Reproduced both prior failures against the old source, ran all tests and checks, built and ran a fresh signed binary, and added no new tests. |
| 2026-08-14 | `codex/review-lifecycle-final` | User visible behavior and lifecycle | `gpt-5.6-sol`, high | Merge ready | 0 | 0 | Reproduced normal tests, race tests, `make check`, fresh `make build`, red-state history and cap failures, and a clean merge tree. |
| 2026-08-16 | `proof/usage-exceeded-notice` | Live failure reporting | Live deployment | Verified | 0 | 0 | Deployed probe on pull request 53. Head `72092d2` created review `4947370530` reading `Review stopped: the model provider reported no remaining usage.` in both the check title and the comment, carrying the provider sentence `The usage limit has been reached`. Head `aa79cea` rewrote that same review in place, proving one comment and an unblocked retry. No issue comment was posted. The successful rewrite is covered by test, because the Codex usage does not reset. |
| 2026-08-19 | `proof/fallback-provider` | Live fallback provider | Live deployment | Verified | 0 | 0 | Deployed probe on pull request 56. The primary provider still refused with `The usage limit has been reached`, proven on head `8ab2a13`. Head `7f179e2` reached `success` and published review `4973535456` reading `Approved`, so the fallback answered. The whole review took 10 seconds including the refused primary call. The pull request names no provider. |
| 2026-08-19 | `proof/review-details-truncation` | Live review details and truncation recovery | Live deployment | Verified | 0 | 0 | Deployed probe on pull request 60. Head `85c3706` published the review details table naming `gpt-5.4-nano` and an `8` second duration. Head `aee4d3b` reviewed 41 files across 5 chunks and reported 105 findings in 409 seconds, and head `c6f994a` reviewed 21 files across 3 chunks in 146 seconds. Both reached `success`, where the same multi chunk shape failed outright before the split. The split path itself is covered by test, because container logs do not reach the worker tail and the check output cannot separate a skipped chunk from a model reporting incomplete coverage. |
| 2026-09-03 | `fix/start-state` | User visible review lifecycle | Codex | Merge ready | 1 | 0 | Live proof showed the start body carrying the previous run and `done` state. The regression test reproduced that mismatch. The fix records the current run as `reviewing` while preserving the reviewed baseline. Merge tree, fresh build, full tests, race tests, and Worker tests passed. |
| 2026-09-06 | `fix/start-state` | Prompt sizing and review lifecycle | Codex | Merge ready | 1 | 0 | Pull request 95 showed two small hunks marked unreadable because each repeated a 267 KB file. Exact hunk slicing broke 18 invariant tests. Bounded context preserved full files that fit, kept true oversized hunks unreadable, and passed the merge tree, fresh build, full tests, race tests, and Worker tests. |
| 2026-09-06 | `fix/start-state` | Model request cardinality | Codex | Merge ready | 6 | 0 | The client could resend one model request through both SDK and stream retry layers. The new tests failed with three requests and now prove one request for an HTTP failure and one broken stream. Provider outages tell readers to wait for recovery before the next push, keep each failed chunk pending, and submit no review. Gateway metadata parsing handles case variants without trusting request text, and deadlines remain timeouts. The merge tree, fresh build, full tests, race tests, and Worker tests passed. |
| 2026-09-12 | `codex/reconcile-current-pr-state` | User visible review reconciliation | Codex | Merge ready | 1 | 0 | Pull request 339 reproduced two fixed inline findings that stayed open after a restack. The affected tests and live merge simulation passed. Live pull request verification follows deployment. |
| 2026-09-13 | `codex/fix-unread-content-verdict` | Content collection and review failure isolation | Codex, bounded adversarial review | Merge ready within reviewed scope | 1 test expectation corrected | 0 | Original-source overlays reproduced unsupported submodule content, swallowed collection errors, and false requested changes. Fresh test executables passed client, collector, recovery, substantive submodule finding, and structural omission decisions. Eight malformed metadata cases passed. A test incorrectly required dismissal instead of a superseding approval; corrected and rerun. Live merge simulation passed. Full gates and deployment belong to the coordinating review. |
| 2026-09-13 | `codex/require-actionable-inline-finding` | Inline verdict invariant and refresh | Codex, bounded adversarial review | Merge ready within reviewed scope | 0 B / 2 SF / 0 N, corrected | 0 | Zero-finding omission reproduced REQUEST_CHANGES before correction. A fresh review executable ran 11 scoped tests and 11 subcases; focused race tests passed. Final-report thread resolution withdrew only the unsupported own block, preserving own approval and another reviewer's block. Repeated fallback refresh stayed nonpassing without approval. Failed reads preserved reviews. Contradictory coverage and failure documentation was corrected. The live trunk still equals the branch base; the uncommitted patch and deployment remain coordinating gates. The reviewer authored the omission tests. |
| 2026-09-13 | `codex/require-actionable-inline-finding` | Failed verdict recovery follow-up | Codex, bounded adversarial review | Merge ready within reviewed scope | 0 | 0 | Independently reran dismissal-denied recovery and three ordinary approval controls. Added public-service attacks proving foreign-author and different-head withheld summaries cannot withhold this head. All five tests and both attack subcases passed; focused race tests passed. Recovery's red state was coordinator-reproduced, not rerun by this reviewer. Deployment remains unverified here. |
